using System;
using System.Collections.Generic;
using System.Text;

namespace domainAnalyserSat
{

    //stores which user is currenly logged in and stores the associated userId.
    
   public static class appState
    {
       public static long currentUserId { get; set; } = 0; //default to 0 when no user logged in}

        public static long currentSessionId { get; set; } = 0; //default to 0 when no user logged in









    }

     
}
